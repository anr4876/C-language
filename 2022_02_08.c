#include <stdio.h>


int main(void) {

	// 대표적인 자료구조 - 연결 리스트
	
	// 대입 연산이 아닌 초기화 연산. 초기화 연산은 선언으로 들어감
	char str1[] = { 'a', 'b', 'c', '\0' };
	char str2[] = { 97, 98, 99, 0 };
	char str3[] = "abc";  // 배열이름(변수) -> 수정불가

	10; // 리터럴 값(상수)
	10.5;
	"abcd"; // 리터럴 문자열 (주소값)

		return 0;
}